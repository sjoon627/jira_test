name: Sync Issue Status with Jira

on:
  issues:
    types: [closed, reopened]
  pull_request:
    types: [opened, closed]

jobs:
  sync-status:
    runs-on: ubuntu-latest
    steps:
      - name: 🔑 Login to Jira
        uses: atlassian/gajira-login@v3
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}

      - name: ✂️ Extract Jira key from title
        id: extract
        continue-on-error: true
        run: |
          title="${{ github.event.issue.title || github.event.pull_request.title || 'No title' }}"
          echo "🔍 Processing title: '$title'"

          # Use Python for robust regex matching
          jira_key=$(echo "$title" | python3 -c "
          import re
          import sys
          title = sys.stdin.read().strip()
          match = re.search(r'([A-Z][A-Z0-9]+-\d+)', title)
          if match:
              print(match.group(1))
          ")

          if [ -n "$jira_key" ]; then
            echo "✅ Found Jira key: $jira_key"
            echo "jira_key=$jira_key" >> $GITHUB_OUTPUT
          else
            echo "⚠️ No Jira key found in title"
            echo "jira_key=" >> $GITHUB_OUTPUT
          fi

      - name: 🔄 Get Jira transitions
        id: transitions
        if: steps.extract.outputs.jira_key
        continue-on-error: true
        run: |
          jira_key="${{ steps.extract.outputs.jira_key }}"
          echo "🔍 Getting transitions for: $jira_key"

          # Debug: Show exact URL and auth
          jira_url="${{ secrets.JIRA_BASE_URL }}"
          echo "🔗 API URL: ${jira_url}/rest/api/2/issue/$jira_key/transitions"

          # Create auth header separately for debugging (remove newlines)
          auth_token=$(echo -n '${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}' | base64 | tr -d '\n')
          echo "🔐 Auth token length: ${#auth_token}"

          # Try curl without --fail-with-body for compatibility
          response=$(curl -s -w "\nHTTP_STATUS:%{http_code}" \
            -H "Authorization: Basic $auth_token" \
            -H "Accept: application/json" \
            "${jira_url}/rest/api/2/issue/$jira_key/transitions")

          curl_exit_code=$?
          http_status=$(echo "$response" | grep -o "HTTP_STATUS:[0-9]*" | cut -d: -f2 || echo "000")
          
          echo "🔍 Curl exit code: $curl_exit_code"
          echo "🔍 HTTP status: $http_status"

          if [ $curl_exit_code -ne 0 ]; then
            echo "❌ Curl failed with exit code: $curl_exit_code"
            echo "📝 Response: $response"
            
            echo "done_id=" >> $GITHUB_OUTPUT
            echo "todo_id=" >> $GITHUB_OUTPUT
            echo "in_progress_id=" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Check HTTP status
          if [ "$http_status" != "200" ]; then
            echo "❌ HTTP error: $http_status"
            echo "📝 Response body: $(echo "$response" | sed 's/HTTP_STATUS:[0-9]*$//')"
            
            echo "done_id=" >> $GITHUB_OUTPUT
            echo "todo_id=" >> $GITHUB_OUTPUT
            echo "in_progress_id=" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Parse JSON response
          transitions_json=$(echo "$response" | sed 's/HTTP_STATUS:[0-9]*$//')
          echo "📝 Available transitions:"
          echo "$transitions_json" | python3 -c "import json, sys; data = json.loads(sys.stdin.read()); [print(f\"  - {t['id']}: {t['name']}\") for t in data.get('transitions', [])]"

          # Extract transition IDs using Python (one-liner)
          done_id=$(echo "$transitions_json" | python3 -c "import json, sys, re; data = json.loads(sys.stdin.read()); print(next((t['id'] for t in data.get('transitions', []) if re.search(r'Done|완료|Closed|종료', t['name'], re.I)), ''))")
          in_progress_id=$(echo "$transitions_json" | python3 -c "import json, sys, re; data = json.loads(sys.stdin.read()); print(next((t['id'] for t in data.get('transitions', []) if re.search(r'In Progress|진행 중|진행중', t['name'], re.I)), ''))")
          todo_id=$(echo "$transitions_json" | python3 -c "import json, sys, re; data = json.loads(sys.stdin.read()); print(next((t['id'] for t in data.get('transitions', []) if re.search(r'To Do|할 일|대기', t['name'], re.I)), ''))")

          echo "🔍 Found transitions - Done: ${done_id:-N/A}, In Progress: ${in_progress_id:-N/A}, To Do: ${todo_id:-N/A}"

          echo "done_id=$done_id" >> $GITHUB_OUTPUT
          echo "in_progress_id=$in_progress_id" >> $GITHUB_OUTPUT
          echo "todo_id=$todo_id" >> $GITHUB_OUTPUT

      - name: 📊 Show current Jira issue status
        if: steps.extract.outputs.jira_key
        continue-on-error: true
        run: |
          jira_key="${{ steps.extract.outputs.jira_key }}"
          echo "🔍 Getting current status for: $jira_key"

          response=$(curl -s -w "HTTP_STATUS:%{http_code}" \
            -H "Authorization: Basic $(echo -n '${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}' | base64 | tr -d '\n')" \
            -H "Accept: application/json" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/$jira_key?fields=status")

          curl_exit_code=$?
          http_status=$(echo "$response" | grep -o "HTTP_STATUS:[0-9]*" | cut -d: -f2 || echo "000")

          if [ $curl_exit_code -eq 0 ] && [ "$http_status" = "200" ]; then
            status_json=$(echo "$response" | sed 's/HTTP_STATUS:[0-9]*$//')
            current_status=$(echo "$status_json" | python3 -c "import json, sys; data = json.loads(sys.stdin.read()) if sys.stdin else {}; print(data.get('fields', {}).get('status', {}).get('name', 'Unknown'))")
            echo "📊 Current Jira status: $current_status"
          else
            echo "::warning::Could not get current status (exit: $curl_exit_code, status: $http_status)"
          fi

      # Issue closed → Jira Done
      - name: ✅ Mark Jira issue as Done (Issue closed)
        if: github.event.action == 'closed' && github.event.issue && steps.extract.outputs.jira_key && steps.transitions.outputs.done_id
        continue-on-error: true
        run: |
          jira_key="${{ steps.extract.outputs.jira_key }}"
          transition_id="${{ steps.transitions.outputs.done_id }}"
          echo "✅ Marking $jira_key as Done (transition: $transition_id)"

          response=$(curl -s -w "HTTP_STATUS:%{http_code}" -X POST \
            -H "Authorization: Basic $(echo -n '${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}' | base64 | tr -d '\n')" \
            -H "Content-Type: application/json" \
            -d "{\"transition\":{\"id\":\"$transition_id\"}}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/$jira_key/transitions")

          curl_exit_code=$?
          http_status=$(echo "$response" | grep -o "HTTP_STATUS:[0-9]*" | cut -d: -f2 || echo "000")

          if [ $curl_exit_code -eq 0 ] && [ "$http_status" = "204" ]; then
            echo "✅ Successfully marked $jira_key as Done"
          else
            echo "::error::Failed to mark as Done (exit: $curl_exit_code, status: $http_status)"
            echo "Response: $(echo "$response" | sed 's/HTTP_STATUS:[0-9]*$//')"
          fi

      # Issue reopened → Jira To Do
      - name: 🔄 Mark Jira issue as To Do (Issue reopened)
        if: github.event.action == 'reopened' && steps.extract.outputs.jira_key && steps.transitions.outputs.todo_id
        continue-on-error: true
        run: |
          jira_key="${{ steps.extract.outputs.jira_key }}"
          transition_id="${{ steps.transitions.outputs.todo_id }}"
          echo "🔄 Marking $jira_key as To Do (transition: $transition_id)"

          response=$(curl -s -w "HTTP_STATUS:%{http_code}" -X POST \
            -H "Authorization: Basic $(echo -n '${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}' | base64 | tr -d '\n')" \
            -H "Content-Type: application/json" \
            -d "{\"transition\":{\"id\":\"$transition_id\"}}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/$jira_key/transitions")

          curl_exit_code=$?
          http_status=$(echo "$response" | grep -o "HTTP_STATUS:[0-9]*" | cut -d: -f2 || echo "000")

          if [ $curl_exit_code -eq 0 ] && [ "$http_status" = "204" ]; then
            echo "✅ Successfully marked $jira_key as To Do"
          else
            echo "::error::Failed to mark as To Do (exit: $curl_exit_code, status: $http_status)"
            echo "Response: $(echo "$response" | sed 's/HTTP_STATUS:[0-9]*$//')"
          fi

      # PR opened → Jira In Progress
      - name: 🚀 Mark Jira issue as In Progress (PR opened)
        if: github.event.action == 'opened' && github.event.pull_request && steps.extract.outputs.jira_key && steps.transitions.outputs.in_progress_id
        continue-on-error: true
        run: |
          jira_key="${{ steps.extract.outputs.jira_key }}"
          transition_id="${{ steps.transitions.outputs.in_progress_id }}"
          echo "🚀 Marking $jira_key as In Progress (transition: $transition_id)"

          response=$(curl -s -w "HTTP_STATUS:%{http_code}" -X POST \
            -H "Authorization: Basic $(echo -n '${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}' | base64 | tr -d '\n')" \
            -H "Content-Type: application/json" \
            -d "{\"transition\":{\"id\":\"$transition_id\"}}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/$jira_key/transitions")

          curl_exit_code=$?
          http_status=$(echo "$response" | grep -o "HTTP_STATUS:[0-9]*" | cut -d: -f2 || echo "000")

          if [ $curl_exit_code -eq 0 ] && [ "$http_status" = "204" ]; then
            echo "✅ Successfully marked $jira_key as In Progress"
          else
            echo "::error::Failed to mark as In Progress (exit: $curl_exit_code, status: $http_status)"
            echo "Response: $(echo "$response" | sed 's/HTTP_STATUS:[0-9]*$//')"
          fi

      # PR merged → Jira Done
      - name: ✅ Mark Jira issue as Done (PR merged)
        if: github.event.action == 'closed' && github.event.pull_request && github.event.pull_request.merged == true && steps.extract.outputs.jira_key && steps.transitions.outputs.done_id
        continue-on-error: true
        run: |
          jira_key="${{ steps.extract.outputs.jira_key }}"
          transition_id="${{ steps.transitions.outputs.done_id }}"
          echo "✅ Marking $jira_key as Done (PR merged, transition: $transition_id)"

          response=$(curl -s -w "HTTP_STATUS:%{http_code}" -X POST \
            -H "Authorization: Basic $(echo -n '${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}' | base64 | tr -d '\n')" \
            -H "Content-Type: application/json" \
            -d "{\"transition\":{\"id\":\"$transition_id\"}}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/$jira_key/transitions")

          curl_exit_code=$?
          http_status=$(echo "$response" | grep -o "HTTP_STATUS:[0-9]*" | cut -d: -f2 || echo "000")

          if [ $curl_exit_code -eq 0 ] && [ "$http_status" = "204" ]; then
            echo "✅ Successfully marked $jira_key as Done (PR merged)"
          else
            echo "::error::Failed to mark as Done (exit: $curl_exit_code, status: $http_status)"
            echo "Response: $(echo "$response" | sed 's/HTTP_STATUS:[0-9]*$//')"
          fi

      - name: 📋 Workflow Summary
        if: always()
        run: |
          echo "## 📋 Jira Sync Workflow Summary"
          echo "**Event:** ${{ github.event_name }} - ${{ github.event.action }}"
          echo "**Title:** ${{ github.event.issue.title || github.event.pull_request.title || 'No title' }}"
          echo "**Jira Key:** ${{ steps.extract.outputs.jira_key || 'Not found' }}"
          echo "**Available Transitions:** Done=${{ steps.transitions.outputs.done_id || 'N/A' }}, Todo=${{ steps.transitions.outputs.todo_id || 'N/A' }}, InProgress=${{ steps.transitions.outputs.in_progress_id || 'N/A' }}"

          if [ -n "${{ steps.extract.outputs.jira_key }}" ]; then
            echo "✅ Jira key extracted successfully"
          else
            echo "⚠️ No Jira key found - check title format (should contain: PROJ-123)"
          fi
